import styles from "./Header.module.scss";
import images from "./../../../public/assets/image/1.png";

function Header() {
    return(
        <header className={styles.header}>
            
        </header>
    );
}

export default Header;